interface AuthProps {
  redirectTo: string;
}

const Auth: React.FC<AuthProps> = ({ redirectTo }) => {
  // Your existing Auth component code
  return (
    // Your JSX
    <></>
  );
};

export default Auth;
