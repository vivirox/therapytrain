interface Props {
    redirectTo: string;
}

const SomeComponent: React.FC<Props> = ({ redirectTo }) => {
    // Component logic here
    return (
    <div>
            {/* JSX here */}
        </div>
    );
}