<!DOCTYPE html>
<html lang="{{lang}}" dir="{{dir}}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="color-scheme" content="light dark">
    <title>{{title}}</title>
    <style>
        /* Base styles with accessibility considerations */
        :root {
            color-scheme: light dark;
            supported-color-schemes: light dark;
        }
        
        body {
            margin: 0;
            padding: 0;
            width: 100%;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            font-size: 16px;
            line-height: 1.5;
            color: #1a1a1a;
            background-color: #ffffff;
        }

        @media (prefers-color-scheme: dark) {
            body {
                color: #ffffff;
                background-color: #1a1a1a;
            }
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }

        .header {
            text-align: center;
            padding: 20px 0;
        }

        .content {
            padding: 20px;
            background-color: #ffffff;
            border-radius: 8px;
        }

        @media (prefers-color-scheme: dark) {
            .content {
                background-color: #2a2a2a;
            }
        }

        .footer {
            text-align: center;
            padding: 20px;
            font-size: 14px;
            color: #666666;
        }

        @media (prefers-color-scheme: dark) {
            .footer {
                color: #999999;
            }
        }

        /* Accessibility-focused styles */
        a {
            color: #0066cc;
            text-decoration: underline;
            word-break: break-word;
        }

        @media (prefers-color-scheme: dark) {
            a {
                color: #66b3ff;
            }
        }

        img {
            max-width: 100%;
            height: auto;
            border: 0;
        }

        .button {
            display: inline-block;
            padding: 12px 24px;
            background-color: #0066cc;
            color: #ffffff;
            text-decoration: none;
            border-radius: 4px;
            font-weight: 600;
            margin: 16px 0;
        }

        @media (prefers-color-scheme: dark) {
            .button {
                background-color: #66b3ff;
                color: #1a1a1a;
            }
        }

        /* High contrast mode styles */
        @media (forced-colors: active) {
            .button {
                border: 2px solid currentColor;
            }
        }

        /* Responsive design */
        @media screen and (max-width: 600px) {
            .container {
                width: 100% !important;
                padding: 10px !important;
            }
            
            .content {
                padding: 15px !important;
            }
        }

        /* Improved readability for visually impaired */
        .visually-hidden {
            position: absolute;
            width: 1px;
            height: 1px;
            padding: 0;
            margin: -1px;
            overflow: hidden;
            clip: rect(0, 0, 0, 0);
            white-space: nowrap;
            border: 0;
        }
    </style>
</head>
<body>
    <div role="presentation" class="container">
        <div role="banner" class="header">
            {{#if logoUrl}}
                <img src="{{logoUrl}}" alt="{{companyName}} Logo" width="150" height="auto">
            {{else}}
                <h1>{{companyName}}</h1>
            {{/if}}
        </div>

        <main role="main" class="content">
            {{{content}}}
        </main>

        <footer role="contentinfo" class="footer">
            <p>{{companyName}} - {{currentYear}}</p>
            {{#if unsubscribeUrl}}
                <p><a href="{{unsubscribeUrl}}" aria-label="Unsubscribe from our emails">Unsubscribe</a></p>
            {{/if}}
        </footer>
    </div>
</body>
</html> 